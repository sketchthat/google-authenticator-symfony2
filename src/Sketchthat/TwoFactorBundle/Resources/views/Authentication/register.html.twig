{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <h3>Register</h3>
            {{ form(register) }}
        </div>
    </div>

    <div class="text-right">
        <a href="{{ path('login') }}">Already registered? Login now</a>
    </div>
{% endblock %}